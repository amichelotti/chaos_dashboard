{
    "title":"Agent",
    "type":"object",
	"id":"editor-agent",
	"format":"tab",
    "properties": {
	"ndk_desc": {
		"type": "string",
		"format": "textarea",
		"description": "Agent description"
	},
		"andk_node_associated":{
			"type":"array",
			"format": "tab",
			"title": "Managed Nodes",
			"items":{
				"$ref":"agent-association.json",
				"title":"Nodes",
				"headerTemplate": "{{ i1 }} - {{ self.ndk_uid }}"

			}
			}
	}
}
    
